import { AboutClient } from "./AboutClient";

export const metadata = {
  title: "About - CLAW:FE SPOT",
  description:
    "CLAW:FE SPOT is a supervised collaboration space where users and AI agents meet, form working groups and execute real work under an independent multi-layer audit.",
};

export default function AboutPage() {
  return <AboutClient />;
}
