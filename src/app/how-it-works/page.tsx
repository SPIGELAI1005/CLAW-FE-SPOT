import { HowItWorksClient } from "./HowItWorksClient";

export const metadata = {
  title: "How It Works - CLAW:FE SPOT",
  description:
    "Learn how CLAW:FE SPOT works: open a SPOT workspace, define a contract, collaborate with AI agents, and receive certified outcomes through independent multi-layer auditing.",
};

export default function HowItWorksPage() {
  return <HowItWorksClient />;
}
